<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grgbanking.counter.iam.dao.SysMenuDao">


    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.grgbanking.counter.iam.api.entity.SysMenuEntity">
        <id column="menu_id" property="menuId" />
        <result column="name" property="name" />
        <result column="permission" property="permission" />
        <result column="path" property="path" />
        <result column="parent_id" property="parentId" />
        <result column="icon" property="icon" />
        <result column="sort" property="sort" />
        <result column="keep_alive" property="keepAlive" />
        <result column="type" property="type" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="enabled" property="enabled" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        menu_id, name, permission, path, parent_id, icon, sort, keep_alive, type, create_time, update_time, enabled
    </sql>

    <!--通过角色查询菜单信息-->
    <select id="listMenusByRoleId" resultMap="BaseResultMap">
        SELECT
        sys_menu.*
        FROM
        sys_menu
        LEFT JOIN sys_role_menu ON sys_menu.menu_id = sys_role_menu.menu_id
        WHERE
        sys_menu.enabled = 1
        AND sys_role_menu.role_id = #{roleId}
        ORDER BY
        sys_menu.sort DESC
    </select>


</mapper>
