<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.grgbanking.counter.iam.dao.SysBizConfDao">

    <resultMap type="com.grgbanking.counter.iam.entity.SysBizConfEntity" id="sysBizConfMap">
        <result property="id" column="ID"/>
        <result property="bizCode" column="BIZ_CODE"/>
        <result property="bizInfo" column="BIZ_INFO"/>
        <result property="appType" column="APP_TYPE"/>
        <result property="createdBy" column="CREATED_BY"/>
        <result property="creationDate" column="CREATION_DATE"/>
        <result property="lastUpdatedBy" column="LAST_UPDATED_BY"/>
        <result property="lastUpdateDate" column="LAST_UPDATE_DATE"/>
        <result property="operationId" column="OPERATION_ID"/>
    </resultMap>

    <select id="getPanelBizInfoList" parameterType="java.lang.String" resultType="com.grgbanking.counter.iam.api.vo.SysBizConfVo">
        select * from SYS_BIZ_CONF
        <where>
            <if test="appType != null and appType != ''">
                AND APP_TYPE = #{appType}
            </if>
            <if test="bizNo != null and bizNo != ''">
                AND (upper(BIZ_CODE) like  upper(CONCAT('%',CONCAT(#{bizNo},'%'))) OR BIZ_INFO like  CONCAT('%',CONCAT(#{bizNo},'%')))
            </if>
                AND IS_ENABLED = '1'
        </where>
    </select>


</mapper>